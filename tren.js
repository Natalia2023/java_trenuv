function makeMessage (name, price) {
        const message = `You picked ${name} , price per item is ${price} credits`;
    
    // const makeMessage = `You picked ${Scanner}, price per item is 3500 credits`;
    
    return ( message );
  };